import test from 'node:test';import assert from 'node:assert/strict';import {DEFAULT_STATE} from '../data/catalog.js';import {validateState,validationStatus} from '../core/validation.js';const state=(patch={})=>({...structuredClone(DEFAULT_STATE),...patch,modules:{...DEFAULT_STATE.modules,...(patch.modules||{})}});test('default general state is not blocked',()=>assert.equal(validationStatus(state()).blocked,false));test('daylight at night is detected',()=>assert.ok(validateState(state({time:'night',lightSource:'open-sky-daylight',lightDirection:'side',lightFalloff:'broad'})).some(i=>i.code==='LIGHT_TIME_CONFLICT')));test('strict mode blocks physical errors',()=>assert.equal(validationStatus(state({captureType:'front-selfie',distance:500})).blocked,true));test('named city is rejected',()=>assert.ok(validateState(state({location:'custom',customLocation:'شارع في الرياض'})).some(i=>i.code==='NAMED_LOCATION_FORBIDDEN')));
