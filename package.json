{
  "name": "physics-prompt-studio",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check app.js && node --check car-selfie.js",
    "test": "npm run check && node --test tests/*.test.mjs"
  }
}
